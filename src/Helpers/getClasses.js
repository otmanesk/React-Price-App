
const getClasses = (origin, ...added) =>{
    let classes = [];
    classes.push(origin);
    if(added){
        classes.push(added);
    }
    else{

    }

    [origin,...added].
}



export default getClasses;